plugins {
    id "com.android.application"
    id "com.joom.paranoid"
    id "com.google.gms.google-services"
    id "com.google.firebase.crashlytics"
}

android {
    namespace "ro.alynsampmobile.launcher"
    compileSdk 35

    defaultConfig {
        applicationId "ro.alynsampmobile.launcher"
        minSdk 24
        targetSdk 35
        versionCode 1700
        versionName "17.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

        ndkVersion "25.1.8937393"

        ndk {
            abiFilters.add("armeabi-v7a")
            abiFilters.add("arm64-v8a")
        }
    }

    paranoid {
        applyToBuildTypes = "ALL"
    }

    signingConfigs {
        debug {
            storeFile file("alyn-debug.keystore")
            storePassword "alyn123123"
            keyAlias "alynkey"
            keyPassword "alyn123123"
        }
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    externalNativeBuild {
        cmake {
            path "src/main/cpp/CMakeLists.txt"
        }
    }

    buildFeatures {
        viewBinding true
        buildConfig true
    }

    lint {
        abortOnError false
        checkReleaseBuilds false
    }

    applicationVariants.configureEach { variant ->
        variant.outputs.configureEach { output ->
            outputFileName = "alyn_sampmobile.apk"
        }
    }
}

dependencies {
    implementation fileTree(dir: "libs", includes: ["*.jar"])

    implementation(libs.appcompat)
    implementation(libs.material)
    implementation(libs.activity)
    implementation(libs.constraintlayout)
    implementation(libs.swiperefreshlayout)
    implementation(libs.navigation.fragment)
    implementation(libs.navigation.ui)
    implementation(libs.github.glide)

    implementation(libs.sdp.android)
    implementation(libs.ssp.android)

    implementation(libs.paranoid.gradle.plugin)

    implementation(libs.firebase.analytics)
    implementation(libs.firebase.crashlytics.ndk)

    implementation(libs.applovin.sdk)

    implementation(libs.easypermissions)
    implementation(libs.prdownloader)
    implementation(libs.okhttp)

    testImplementation(libs.junit)
    androidTestImplementation(libs.ext.junit)
    androidTestImplementation(libs.espresso.core)
}
